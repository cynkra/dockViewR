% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{normalize_dock_dims}
\alias{normalize_dock_dims}
\title{Normalise pixel dimensions in a grid list}
\usage{
normalize_dock_dims(x, digits = 2L)
}
\arguments{
\item{x}{A list, typically the result of \code{\link[=get_grid]{get_grid()}}.}

\item{digits}{Number of decimals to round shares to. Default \code{2}.}
}
\value{
The same list with each branch's child \code{size} replaced by its
share of the branch total, the branch's own \code{size} removed, and the
grid-level \code{width} / \code{height} removed.
}
\description{
Replaces absolute pixel sizes in a grid returned by \code{\link[=get_grid]{get_grid()}} by the
share each child takes of its parent branch, rounded to \code{digits} decimals.
The top-level \code{width} and \code{height} (viewport-dependent) are dropped.
}
\details{
Useful when comparing dock state in a \code{{shinytest2}} snapshot: absolute
pixel sizes drift by a few units across Chromium and OS versions (causing
fragile CI failures), while the \emph{relative} layout (the actual invariant
dockview preserves) stays stable and still catches real regressions
(e.g. a panel collapsing to a sliver when it should be half the dock).
}
