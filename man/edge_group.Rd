% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edge_group.R
\name{edge_group}
\alias{edge_group}
\title{Edge group}
\usage{
edge_group(
  id,
  position = c("left", "right", "top", "bottom"),
  initial_size = NULL,
  minimum_size = NULL,
  maximum_size = NULL,
  collapsed = FALSE,
  collapsed_size = NULL,
  ...
)
}
\arguments{
\item{id}{Edge group unique id. Used to reference the group from a
\code{\link[=panel]{panel()}}'s \code{position = list(referenceGroup = ...)}.}

\item{position}{Edge on which to pin the group. One of \code{"left"},
\code{"right"}, \code{"top"}, \code{"bottom"}.}

\item{initial_size}{Initial size of the edge group, in pixels.}

\item{minimum_size}{Minimum size of the edge group, in pixels.}

\item{maximum_size}{Maximum size of the edge group, in pixels.}

\item{collapsed}{Whether the edge group is initially collapsed.}

\item{collapsed_size}{Size of the edge group when collapsed, in pixels.
Defaults to \code{35} on the dockview side.}

\item{...}{Other options forwarded to \code{api.addEdgeGroup()}. See
\url{https://dockview.dev/docs/core/groups/edgeGroups}. Use the
JavaScript option names (camelCase) for these extras.}
}
\value{
A list of class \code{dock_edge_group} with the camelCased options
ready to be sent to the dockview JavaScript API. Contains at least:
\itemize{
\item \code{id}: the edge group id (string).
\item \code{position}: one of \code{"left"}, \code{"right"}, \code{"top"}, \code{"bottom"}.
\item \code{options}: a list of options forwarded to \code{api.addEdgeGroup()}.
}
}
\description{
Create an edge group, that is a \href{https://dockview.dev/docs/core/groups/edgeGroups}{group}
pinned to one of the four edges (left, right, top, bottom) of a
\code{\link[=dock_view]{dock_view()}}. Edge groups support tabs, drag-and-drop, overflow and the
full group panel API. They cannot be maximised, floated or popped out.
}
\details{
Pass edge groups to \code{\link[=dock_view]{dock_view()}} through \code{edge_groups}, or add them on
the fly from the server with \code{\link[=add_edge_group]{add_edge_group()}}.
}
\seealso{
\code{\link[=add_edge_group]{add_edge_group()}}, \code{\link[=remove_edge_group]{remove_edge_group()}},
\code{\link[=set_edge_group_visible]{set_edge_group_visible()}}.
}
