# Check whether an object is an edge group

Check whether an object is an edge group

## Usage

``` r
is_edge_group(x)
```

## Arguments

- x:

  An object to test.

## Value

Logical value indicating whether `x` was created with
[`edge_group()`](https://cynkra.github.io/dockViewR/reference/edge_group.md).
